---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: borgbackup_system
    image: centos:7
  - name: borgbackup_upstream
    image: centos:7
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  inventory:
    host_vars:
      borgbackup_upstream:
        borgbackup_install_method: upstream

verifier:
  name: testinfra
  lint:
    name: flake8
